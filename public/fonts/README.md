# Capri Italy font

Place the licensed webfont here as `capri-italy.woff2`. The site is already configured to load that exact path, so no code change is needed.

Use lowercase kebab-case filenames. WOFF2 is preferred for the website; keep the original OTF/TTF outside `public` as the source archive.
